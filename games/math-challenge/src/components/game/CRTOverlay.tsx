/**
 * CRT Overlay - renders scanlines and vignette for retro CRT monitor effect
 */
const CRTOverlay = () => {
  return <div className="crt-overlay" aria-hidden="true" />;
};

export default CRTOverlay;
